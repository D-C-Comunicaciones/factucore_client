import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
  allowedDevOrigins: ["https://lemon-cups-dig.loca.lt", "grab-grant-despite.ngrok-free.dev"],
};

export default nextConfig;